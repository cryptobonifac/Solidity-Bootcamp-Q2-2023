import { expect } from "chai";
import { ethers } from "hardhat";
import { describe } from "mocha";
import { assert } from "console";


describe("TokenizedBallot", async () => {

    beforeEach(async ()=> {
    })

    it("Mocha is working ",async () => {        
        const test = true;
        expect(test).to.be.true; 
    }) 
});    